name: CD
on:
  push:
    branches:
      - seminario-everton
      - master
    pull_request_target:
      types: [assigned, opened, synchronize, reopened]
jobs:
  deploy:
    name: deploy-cdms
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to Server
        run: |
          ssh -i "${{ secrets.AWS_SSH_KEY }}" ubuntu@ec2-3-15-189-73.us-east-2.compute.amazonaws.com
          cd /var/www/cdms/
          git pull origin seminario-everton
          git status